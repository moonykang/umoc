FILE(GLOB SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp ${CMAKE_CURRENT_SOURCE_DIR}/*.mm)
FILE(GLOB HEADERS ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(
    editor
    ${SOURCES} ${HEADERS}
)

TARGET_LINK_LIBRARIES(
    editor
    umoc
    glfw
    imgui
    ${VULKAN_LIBRARY}
)